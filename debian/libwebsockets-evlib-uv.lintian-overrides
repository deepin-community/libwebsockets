# it's a plugin depending on ABI versioned library
lacks-unversioned-link-to-shared-library example: usr/lib/*/libwebsockets-evlib_uv.so [usr/lib/*/libwebsockets-evlib_uv.so]
shared-library-lacks-version usr/lib/*/libwebsockets-evlib_uv.so libwebsockets-evlib_uv.so

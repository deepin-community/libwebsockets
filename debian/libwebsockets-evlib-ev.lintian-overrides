# it's a plugin depending on ABI versioned library
lacks-unversioned-link-to-shared-library example: usr/lib/*/libwebsockets-evlib_ev.so [usr/lib/*/libwebsockets-evlib_ev.so]
shared-library-lacks-version usr/lib/*/libwebsockets-evlib_ev.so libwebsockets-evlib_ev.so
